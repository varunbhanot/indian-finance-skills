{
  "name": "indian-finance-skills",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code skills for Indian personal finance, backed by a deterministic core.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "ctc-decoder": "node src/cli/ctc-decoder.ts",
    "record-transcript": "node scripts/record-transcript.ts",
    "check-transcript": "node scripts/check-transcript.ts",
    "typecheck": "tsc --noEmit",
    "lint": "node scripts/lint-no-floats.ts",
    "test": "node --test 'test/**/*.test.ts'"
  },
  "devDependencies": {
    "@types/node": "~22.20.1",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "yaml": "~2.9.0"
  }
}
